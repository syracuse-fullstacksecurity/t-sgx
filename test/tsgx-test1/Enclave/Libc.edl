/* Libc.edl - EDL sample for trusted C library. */

enclave {
    
    from "sgx_tstdc.edl" import sgx_oc_cpuidex;

    /* 
     * A subset of the C99 standard is supported as well as SGX customized functions: 
     *  sgx_cpuid, etc.
     */

    trusted {
        public int ecall_resort([in,count=size] char *list, [in,count=size] int *order, int size); 
        public int ecall_resort_ncp(long list_addr, long tv_addr, int size); 
    };
};
